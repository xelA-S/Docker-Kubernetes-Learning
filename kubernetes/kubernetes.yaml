apiVersion: 1
kind: Deployment #or Service
metadata:
  name: Something
  #if Deployment then,
  Labels:
#Attributes in the spec are specific to the kind being made
spec:
  replicas: 2 #How many replicas of the app(pod) should be made
  selector:
  template:

  #or if Service
  ports:

#There are 3 parts of a kubernetes configuration:
#Metadata
#Specification(spec)
#Status(Status is auto generated by kubernetes after the app is created and will update automatically, data is received from etcd)
